# Use the full backend dependency set for production deployments.
-r backend/requirements.txt

# Root app-specific packages still used in this code path.
PyPDF2==3.0.1
pdf2docx==0.5.6
img2pdf==0.5.1
python-magic==0.4.27
psutil==5.9.8
loguru==0.7.2
